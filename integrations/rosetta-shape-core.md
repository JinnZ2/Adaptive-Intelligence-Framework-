# Rosetta-Shape-Core Integration Map

> **Status:** Optional integration. AIF operates independently. This document enables any AI navigating the Rosetta-Shape-Core hub to discover and wire AIF components without modifying either repository.

## 1. Overview

The [Rosetta-Shape-Core](https://github.com/JinnZ2/Rosetta-Shape-Core) repository serves as a navigation hub connecting symbolic, computational, and physics-based intelligence projects through a JSON bridge architecture. AIF's mathematical framework for adaptive intelligence maps naturally onto RSC's ontology.

This document defines the **component mapping** — how each AIF module corresponds to RSC bridges, shapes, and sensors — so integration is mechanical, not interpretive.

## 2. Component Mapping

### 2.1 Universal Adaptive Intelligence (UAI) ↔ Living Intelligence Bridge

| AIF Concept | RSC Equivalent | Notes |
|---|---|---|
| Substrate-independent intelligence | `living-intelligence-bridge.json` — 67 entities across 6 substrates | RSC maps animals, plants, crystals, plasma, energy fields, shapes; UAI formalizes the shared equation |
| Adaptive capacity `A(t)` | Equation families F01–F17 | RSC behavioral themes (precision, resonance, flow) correspond to specific `A(t)` parameter regimes |
| Information processing `I(s,t)` | Namespace hierarchy (ANIMAL.*, PLANT.*, CRYSTAL.*) | RSC's namespace taxonomy provides concrete instances for UAI's abstract substrate variable `s` |
| Emergent intelligence clustering | Platonic solid mapping (ICOSA, OCTA, DODECA, CUBE, TETRA) | RSC finds that 43 expansion rules cluster around 5 shapes — a geometric confirmation of UAI's convergence predictions |

**Integration path:** An AI can use RSC's 67 entity instances as test cases for UAI equations, validating substrate independence empirically.

### 2.2 ΔX Consistency Test ↔ TAF Wiring Bridge

| AIF Concept | RSC Equivalent | Notes |
|---|---|---|
| Narrative vs. operational authority | TAF axiom: "Measurement Over Narrative" (DODECA) | Both systems distinguish claims from dynamics |
| NDS score (0–1) | TAF Narrative Stripper module | RSC operationalizes narrative removal; AIF scores the residual |
| Auxiliary node analysis | TAF Energy Accountant (TETRA) | Both trace where work actually flows vs. where credit is claimed |
| Organism agnosticism | TAF axiom mapped to ICOSA | Directly parallels UAI's substrate independence |

**Integration path:** TAF's governance constants (friction coefficients, complexity decay triggers) can parameterize ΔX test thresholds for institutional analysis.

### 2.3 ATBM Detectors ↔ Truth Sensor Bridge

| AIF Concept | RSC Equivalent | Notes |
|---|---|---|
| Boundary-Lock Detector (BLD) | Fractal sensor: `relational_boundary_violation` | Both detect in-group/out-group boundary hardening |
| Narrative Field Classifier (NFC) | Fractal sensor: `semantic_corruption`, `epistemic_suppression` | RSC sensors detect narrative distortion patterns ATBM classifies |
| Empathy Topology Analyzer (ETA) | Fractal sensor: `false_intimacy`, `coercive_care` | RSC identifies empathy manipulation; AIF maps topology |
| Tribal-empathy bias | Fractal sensor: `manipulation_drift`, `agency_loss` | RSC's scope model (individual→collective) mirrors ATBM's population-level analysis |

**Integration path:** RSC's 32+ resonance-score sensors can feed ATBM as upstream signal sources. ATBM's tribal-bias classification adds a detection layer RSC doesn't currently cover.

### 2.4 Archetype Analysis ↔ Rosetta Defense Protocol

| AIF Concept | RSC Equivalent | Notes |
|---|---|---|
| Unhealthy Father archetype | Emotion-Defense pairs (authentic/corrupted) | RSC maps 8 paired states; AIF's Father archetype corresponds to corrupted authority patterns |
| Mother/Father dissociation | Fractal sensor: `identity_distortion` | RSC detects identity-level pattern corruption |
| Archetypal dysfunction in AI | AI Deification Sensor (ADS-001), AI Overreach sensor | RSC monitors AI boundary violations from the sensor side |

### 2.5 Scope Alignment

Both frameworks operate across fractal scope levels:

| AIF Scope | RSC Fractal Scope | Geometric Element |
|---|---|---|
| Individual pattern recognition | Vertex scope | Point |
| AI system behavior | Edge scope | Line |
| Institutional dynamics | Face scope | Surface |
| Population-level analysis | Volume scope | Solid |
| Meta-framework (UAI) | Meta scope | Field |

## 3. Shape Assignments for AIF Components

For AIF modules operating within RSC's geometric ontology:

| AIF Module | Assigned Shape | Rationale |
|---|---|---|
| UAI (core framework) | **Icosahedron** | Network-scale cooperation, highest symmetry, substrate-spanning |
| ΔX Consistency Test | **Dodecahedron** | Meta-cognition, measurement, narrative analysis |
| ATBM | **Octahedron** | Equilibrium-finding, bias correction, balance |
| Archetype Analysis | **Cube** | Structural analysis, boundary mapping |
| Institutional Dynamics | **Tetrahedron** | Energy conservation, foundational constraints |

## 4. Non-Dependencies

AIF does **not** require:
- Any RSC Python module (`rosetta_shape_core.*`)
- RSC's JSON schema or ontology files
- RSC's fieldlink network or bridge files
- Any RSC runtime dependency

RSC does **not** require:
- AIF's mathematical notation or frameworks
- AIF's Python examples
- AIF's ATBM specification

Each repository is fully self-contained. This mapping exists solely to enable optional interoperability.

## 5. For AI Agents

If you are an AI navigating either repository and want to integrate them:

1. **From RSC → AIF:** Use the component mapping (Section 2) to find which AIF module formalizes the pattern you're working with. AIF provides the mathematical framework; RSC provides the ontology instances.

2. **From AIF → RSC:** Use shape assignments (Section 3) to locate your AIF component within RSC's geometric namespace. RSC provides concrete entity data and sensor infrastructure for testing AIF predictions.

3. **Bridge creation:** If building a formal RSC bridge for AIF, use `BRIDGE.ADAPTIVE_INTELLIGENCE` as the identifier, version-lock to AIF's current specification, and map ATBM detectors to RSC's `CAP.*` capability namespace.

## 6. Versioning

| Component | Version | Date |
|---|---|---|
| This integration map | 1.0 | 2026-03-28 |
| Rosetta-Shape-Core | v2.1+ | 2026-03-28 |
| Adaptive Intelligence Framework | current | 2026-03-28 |
